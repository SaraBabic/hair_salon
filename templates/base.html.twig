<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=PT+Sans+Narrow:wght@400;700&display=swap" rel="stylesheet">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-OERcA2EqjJCMA+/3y+gxIOqMEjwtxJY7qPCqsdltbNJuaOe923+mo//f6V8Qbsw3" crossorigin="anonymous"></script>
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}
    </head>
    <body>
    <nav class="navbar navbar-expand-lg" style="background: linear-gradient(to bottom, rgb(117, 187, 191, 1), rgb(117, 187, 191, 0.4));">
        <div class="container-fluid">
            <a class="navbar-brand ms-4" href="{{ path('app_home') }}">
                <img src="{{ asset('build/images/logo.png') }}" alt="hair_salon_logo">
                HairSalons
            </a>
            <div>
                <ul class="navbar-nav me-3 mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link " href="#">About us</a>
                    </li>
                    {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_logout') }}">Logout</a>
                    </li>
                    {% else %}
                    <li class="nav-item">
                        <a class="nav-link " href="{{ path('app_login') }}">Login</a>
                    </li>
                    {% endif %}
                </ul>
            </div>
        </div>
    </nav>
    {% for message in app.flashes('success') %}
        <div class="alert alert-success alert-dismissible mb-0">{{ message }}</div>
    {% endfor %}
    {% for message in app.flashes('error') %}
        <div class="alert alert-danger mb-0">{{ message }}</div>
    {% endfor %}

        {% block body %}{% endblock %}

    <footer>
        <div class="pt-3 ms-4" style="height: 14vh;"><img src="{{ asset('build/images/logo.png') }}" alt="hair_salon_logo"><span class="fw-bold">HairSalons</span></div>
        <div class="bg-dark text-light d-flex justify-content-between" style="height: 31vh;">
            <div class="pt-4">
                <h3 class="ms-3">About Us</h3>
            </div>
            <div class="pt-4">
                <h5>Social Media</h5>
                <ul>
                    <li><a href="#">instagram</a></li>
                    <li><a href="#">facebook</a></li>
                    <li><a href="#">twitter</a></li>
                </ul>
            </div>
            <div class="pt-4">
                <h5>Become a Member</h5>
                <ul>
                    <li><a href="#">I'm customer</a></li>
                    <li><a href="#">I'm salon owner</a></li>
                </ul>
            </div>
            <div>
                <img src="{{ asset('build/images/footer.png') }}" alt="footer_img">
            </div>
        </div>
    </footer>
    {% block javascripts %}
        {{ encore_entry_script_tags('app') }}
    {% endblock %}
    </body>
</html>
